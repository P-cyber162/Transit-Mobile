# TransitOps Driver — Agent Notes

This project targets **Expo SDK 54** (React Native 0.81 / React 19.1).

Read the versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing Expo-specific code.

## Quick start

```bash
npm install --legacy-peer-deps
npm run start:tunnel
```

## Demo driver (dev / seeded backend)

- Email: `kwame.mensah@transitops.local`
- Password: `Driver@12345`
